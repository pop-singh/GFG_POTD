class Solution {
   int solve(int a, int b, int c) {
        // code here
        int max=Math.max(a,Math.max(b,c));
        if((max)>((a+b+c)-max+1)*2)
            return -1;
        return a+b+c;
    }
};
